package com.gym.app.domain.model

/**
 * PHASE 01D — CORE USER MODELS
 *
 * Biological sex used strictly for calculation purposes (e.g. future BMR /
 * calorie / macro calculations). Not a UI concept and not used to limit or
 * assume any other domain behavior.
 */
enum class Gender {
    MALE,
    FEMALE
}
