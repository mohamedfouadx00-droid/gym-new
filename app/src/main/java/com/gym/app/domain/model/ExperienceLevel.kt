package com.gym.app.domain.model

/**
 * PHASE 01D — CORE USER MODELS
 *
 * User's general training experience level. Used later to scale workout
 * complexity/volume — no such logic is implemented in this phase.
 */
enum class ExperienceLevel {
    BEGINNER,
    INTERMEDIATE,
    ADVANCED
}
