package com.gym.app.domain.model

/**
 * PHASE 01D — CORE USER MODELS
 *
 * Where the user primarily trains. Affects which future exercises/equipment
 * are relevant — no such logic is implemented in this phase.
 */
enum class WorkoutLocation {
    HOME,
    GYM,
    OUTDOOR
}
